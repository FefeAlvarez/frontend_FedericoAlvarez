import { colors } from "../../globals.styles";

const badge = {
  width: "18px",
  height: "18px",
  color: colors.white,
  backgroundColor: colors.orangeDark,
};

const iconCart = {
  color: colors.orange,
};

export const styles = {
  badge,
  iconCart,
};
