import { colors } from "../../globals.styles";

const button = { color: colors.white, backgroundColor: colors.orangeDark };
const card = {
  maxWidth: 200,
};
const cover = {
  width: "200px",
  height: "300px",
  objectFit: "contain",
};
const descriptions = {};
const icono = {
  color: colors.white,
};
const title = {
  color: colors.orange,
};

export const styles = {
  card,
  cover,
  title,
  descriptions,
  icono,
  button,
};
