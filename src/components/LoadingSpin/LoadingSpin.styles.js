import { colors } from "../../globals.styles";
const messageText = {
  color: colors.orange,
  fontSize: "2em",
  fontWeight: "bold",
  textAlign: "center",
};

export const styles = {
  messageText,
};
