import { colors } from "../../globals.styles";
const dropdown = {
  marginTop: 5,
};
const link = {
  textDecoration: "none",
  color: colors.orange,
};

export const semiStyles = {
  dropdown,
  link,
};
