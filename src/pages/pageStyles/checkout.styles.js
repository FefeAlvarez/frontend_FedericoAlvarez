import { colors } from "../../globals.styles";

const button = {
  textDecoration: "none",
  color: colors.orange,
  padding: "2em",
  margin: 5,
};

const cardContainer = {
  height: "80vh",
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  alignItems: "center",
};

const link = {
  textDecoration: "none",
  color: colors.orange,
};

export const styles = {
  button,
  cardContainer,
  link,
};
